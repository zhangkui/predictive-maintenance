services:
  predictive-maintenance:
    build: .
    ports:
      - "8080:8080"
    environment:
      HTTP_ADDR: ":8080"
      DB_PATH: "/app/data/predictive-maintenance.db"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
